using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CurveAnimationTest : MonoBehaviour
{
    //public AnimationCurve curve;
    //public float speed = 5.0f;

    //private float time = 0.0f;
    //public float curveValue = 0.0f;

    //private void Start()
    //{
    //}

    //void Update()
    //{
    //    //time = UnityEngine.Random.Range(0,speed);
    //    //curveValue = curve.Evaluate(time);
    //    if (speed > 0)
    //    {
    //        speed -= Time.deltaTime;
    //        transform.Rotate(Vector3.forward, speed); // 在y轴上根据曲线值进行旋转
    //    }

    //    // 可以根据需要在其他方向上进行旋转
    //    // transform.Rotate(Vector3.right, curveValue);
    //    // transform.Rotate(Vector3.forward, curveValue);
    //}
}
